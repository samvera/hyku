<%# OVERRIDE Hyrax v5.3.0. Do not delete as redundant: without it, Hyrax's own
    resource_type partial takes over and offers every work type the general
    list, so an OER work never sees the oer_types its schema cites. %>

<%= render partial: 'records/edit_fields/default', locals: { f: f, key: key } %>
