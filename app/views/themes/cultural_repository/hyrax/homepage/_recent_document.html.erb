<%# OVERRIDE Hyrax v5.0.0rc2 template for client theming and shared search %>
<li class="recently-uploaded col-6 col-md-2">
  <%= link_to [main_app, recent_document] do %>
    <%= render_thumbnail_tag(recent_document, {class: 'img-fluid center-block', alt: thumbnail_alt_text_for(recent_document)}, {suppress_link: true}) %>
  <% end %>
  <div class="recent-doc-title">
    <%= link_to generate_work_url(recent_document, request, params) do %>
      <h3><%= markdown(recent_document.to_s) %></h3>
    <% end %>
  </div>
</li>
